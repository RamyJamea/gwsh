from enum import Enum


class TableEnum(Enum):
    USERS = "users"
    BRANCHES = "branches"
    TABLES = "tables"
    PRODUCTS = "products"
    CATEGORIES = "categories"
    SIZES = "sizes"
    EXTRAS = "extras"
